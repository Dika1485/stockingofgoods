import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.stockingofgoods',
  appName: 'stockingofgoods',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
